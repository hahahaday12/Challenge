import styled from 'styled-components';
import React from 'react';


const Posting = () => {
    return(
        <StyledButton></StyledButton>
    )
}

export default Posting;
// react router 로 화면 바뀌어 줘야 한당. 

const StyledButton = styled.button`

`

